# Drupal API
API_URL=https://localhost:8080
APP_ENV=dev

# UAT Push (translations import) - optional, only works in development (APP_ENV=dev)
# Can be removed/disabled after development
UAT_TARGET_URL=https://your-uat-app.example.com/api/translations/import
UAT_SHARED_SECRET=change-me-in-uat
UAT_CLIENT_ID=json-translator
